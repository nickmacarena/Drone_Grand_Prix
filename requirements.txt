pymavlink<2.4.45  # newer versions pull in fastcrc (Rust), no prebuilt win_arm64 wheel for Py3.14

# Vision deps deferred until VQ2. opencv-python pulls in an older numpy that has
# no prebuilt win_arm64 wheel for Python 3.14, requiring MSVC to build.
# opencv-python
# numpy
